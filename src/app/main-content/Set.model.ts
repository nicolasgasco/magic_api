export class Set {
  // Public is to avoid the repetition of declaring and adding to constructor
  constructor(
    public name: string,
    public code: string,
    public type: string,
    public year: number
  ) {}
}
